#include "Fibo.hpp"
#include <iostream>
#include <cassert>

int main() {
  for (int i=0; i<50; i++){
      assert(fibo(i) >= 0);
      assert(fibo(i) > fibo(i-1));
      std::cout << fibo(i) << std::endl;
  }
    return 0;
}

